{
  "production": {
    "url": "https://rafaelcardoso.co.uk",
    "status": "live",
    "deployment_method": "traefik_reverse_proxy",
    "traefik": {
      "domains": ["rafaelcardoso.co.uk", "www.rafaelcardoso.co.uk"],
      "network": "proxy",
      "ssl_cert_resolver": "cloudflare",
      "container_port": 3000
    },
    "server": {
      "host": "localhost",
      "port": 3000,
      "docker_container": "masterpage",
      "docker_network": "proxy",
      "docker_ip": "172.18.0.13",
      "docker_compose_file": "docker-compose.yml",
      "commands": {
        "start": "docker compose up -d",
        "stop": "docker compose down",
        "restart": "docker compose restart",
        "rebuild": "docker compose up -d --build",
        "logs": "docker logs -f masterpage"
      }
    },
    "repository": {
      "url": "https://github.com/RafaelCardoso92/masterpage.git",
      "branch": "master",
      "commands": {
        "push": "git push origin master",
        "pull": "git pull origin master",
        "status": "git status"
      }
    }
  },
  "local": {
    "url": "http://localhost:3000",
    "commands": {
      "dev": "npm run dev",
      "build": "npm run build",
      "start": "npm start"
    }
  },
  "notes": [
    "Masterpage is served via Traefik reverse proxy at rafaelcardoso.co.uk",
    "Traefik handles HTTPS and SSL certificates via Cloudflare resolver",
    "Docker container auto-restarts unless stopped manually",
    "Container uses 'proxy' network for Traefik communication",
    "ssh.rafaelcardoso.co.uk is a separate Cloudflare Tunnel for SSH access (not masterpage)",
    "Always test locally before deploying to production"
  ]
}
